---
interface Props {
  label: string;
  value: string;
}

const { label, value } = Astro.props;
---

<div class="flex flex-col gap-1">
  <span class="font-display text-3xl font-bold text-ink-900">{value}</span>
  <span class="text-sm text-graphite leading-snug">{label}</span>
</div>